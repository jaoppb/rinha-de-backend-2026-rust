[package]
name = "api"
version = "0.1.0"
edition = "2024"

[dependencies]
memmap2 = "0.9.10"
io-uring = "0.6"
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

[features]
default = []
verbose-logging = []
